# General
alias bm="branch_merged"
alias dots="cd ~/workspace/dot_files"

# Work
alias loop="cd ~/workspace/loop"
alias apps="cd ~/workspace/loop/returns-argocd-apps"

# Ansible
alias a="ansible"
alias ap="ansible-playbook"
alias dodots="ansible-playbook -c local -K -i localhost, deploy.yaml"

# Kubernetes
alias kc="kubectl"
alias kcx="kubectx"

# Terraform
alias tf="terraform"
alias tfp="terraform plan"
