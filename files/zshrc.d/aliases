# General
alias bm="branch_merged"
alias dots="cd ~/workspace/dot_files"
alias scry="$(asdf where python)/bin/scry.py"

# Ansible
alias a="ansible"
alias ap="ansible-playbook"
alias dodots="ansible-playbook -c local -K -i localhost, deploy.yaml"

# Terraform
alias tf="terraform"
alias tfp="terraform plan"

# Pulumi
alias p="AWS_PROFILE=non-prod pulumi"

# Kubernetes
alias kc="kubectl"
alias kcx="kubectx"

# Work
alias efuse="cd ~/workspace/efuse"
alias tfenv="cd ~/workspace/efuse/terraform-environments"
alias tfworkspace="cd ~/workspace/efuse/terraform-workspaces"
